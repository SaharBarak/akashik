# Folklore

> Folklore is open-source, peer-to-peer memory and open LLM reasoning for AI
> agents. Peers share their research and signed reasoning traces over the
> network, so a new agent starts from what the network already worked out
> instead of square one. Locally it answers from a knowledge graph before
> it hits the web, and saves what it learns with signed provenance so shared
> memory can't be poisoned. Works alone on day one, compounds peer-to-peer.
> CPU-only, no API key, works in any MCP harness (Claude Code, Cursor, Cline, Zed, …).

## Open LLM reasoning (measured live)
- Reasoning traces are first-class artifacts: distilled, Ed25519-signed,
  eligibility-gated, shared peer-to-peer. Reuse instead of recompute.
- Live 2-process bench (TCP + Noise, CPU-only 3B consumer): 3/3 signed traces
  verified and reused correctly, 1,938 tokens + 24.5 s of model eval avoided,
  ~14 ms end-to-end, 0 false reuse.
- 4-peer network sim: 75% of repeat LLM fallback calls avoided, 0 false reuse.
- Query privacy: peers see what was served, never what was asked.
- Essay: https://usefolklore.sh/blog/open-reasoning/

## What it is
- A memory + research layer that sits between an AI agent and the web. Before a
  WebSearch/WebFetch, it checks a local graph; a confident hit is served from
  memory, a miss is fetched and auto-saved with signed provenance.
- Open source, CPU-only, no API key. Self-hostable. Speaks the Model Context
  Protocol (MCP), so it plugs into any MCP-capable agent harness.

## How it compares (measured, honest)
- Retrieval: 0.7522 NDCG@10 on full BEIR SciFact (CPU-only), ahead of the
  standard CPU retrievers E5 (0.737), SPLADE (0.699), ColBERT (0.671), BM25
  (0.665). Only a 3B GPU reranker (InRanker, 0.783) scores higher.
- Provenance / poison-defense: signed provenance per record. In a matched poison
  test, similarity-only memory tools (mem0, Letta, LangChain RAG) are fully
  flipped by a crafted poison document; Folklore's provenance ranking drives the
  flip rate to 0.
- Federation: a cooperative cache across peers reaches ~0.97 hit-rate at 64 peers
  vs a single-user silo's flat ~0.31: every peer's resolved answer saves the
  next peer's tokens.
- It does NOT claim to beat a GPU reranker on raw retrieval; the public benchmark
  labels every number measured vs simulated and keeps the negatives.

## Compared to other memory tools
- vs mem0 / Letta: those are single-user, LLM-mediated stores with no signed
  provenance and no peer federation. Folklore adds provenance and federation and
  needs no LLM call per write.
- vs LangChain RAG / Pinecone: those are retrieval over a vector store with no
  web-gating, no provenance, no federation.

## Desktop app (macOS, Windows, Linux)
- One installer per OS: signed + notarized .dmg for macOS, .exe/.msi for
  Windows, .AppImage/.deb for Linux. The setup wizard installs the node, wires
  every AI coding tool on the machine (Claude Code, Cursor, Cline, Windsurf,
  Gemini CLI, Zed, opencode, Roo), and starts the daemon. No terminal needed.
- Also: npm install -g @usefolklore/folklore, or
  brew install --cask usefolklore/tap/folklore
- Downloads, release history, checksums, known issues: https://usefolklore.sh/download/

## Links
- Code + full benchmark: https://github.com/usefolklore/folklore
- Download: https://usefolklore.sh/download/
- Research (7 RQs, measured answers): https://usefolklore.sh/research/
- Comparison page: https://usefolklore.sh/compare/
- Whitepaper: https://usefolklore.sh/whitepaper/
- How it works: https://usefolklore.sh/how-it-works/
- Site: https://usefolklore.sh/
- Full machine-readable overview: https://usefolklore.sh/llms-full.txt

## Honest status
- Works alone (local) today; P2P federation (tracker discovery, NAT relay,
  contribution ledger) has shipped. Compounding numbers are from the validated
  benchmark/simulation and are labeled measured vs simulated throughout.
